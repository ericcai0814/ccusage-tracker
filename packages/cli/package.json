{
  "name": "@ericcai/ccusage-tracker-cli",
  "version": "0.1.0-rc.1",
  "private": false,
  "type": "module",
  "bin": {
    "tracker": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=18"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --target node18 --clean",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.8.0",
    "vitest": "^1.6.0"
  }
}
