{
  "name": "@ccusage-tracker/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "tracker": "./dist/index.js"
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bun-types": "latest",
    "typescript": "^5.8.0"
  }
}
